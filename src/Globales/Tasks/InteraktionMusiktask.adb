pragma SPARK_Mode (On);

package body InteraktionMusiktask is

   -- Doch keine Prozeduren/Funktionen in den Interaktionsdateien anlegen?
   -- Wäre eindeutig kürzer und bei der aktuellen Änderungsquote vielleicht auch besser?
   procedure Leer
   is begin
      
      null;
      
   end Leer;

end InteraktionMusiktask;
