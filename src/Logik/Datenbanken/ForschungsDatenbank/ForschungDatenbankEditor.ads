pragma SPARK_Mode (On);
pragma Warnings (Off, "*array aggregate*");

package ForschungDatenbankEditor is

   procedure ForschungDatenbankEditor;

private

   procedure ForschungDatenbankSpeichern;

end ForschungDatenbankEditor;
