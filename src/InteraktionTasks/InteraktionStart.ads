pragma SPARK_Mode (On);
pragma Warnings (Off, "*array aggregate*");

package InteraktionStart is
   
   ErzeugeFenster : Boolean := False;
   FensterErzeugt : Boolean := False;
   EinlesenAbgeschlossen : Boolean := False;

end InteraktionStart;
