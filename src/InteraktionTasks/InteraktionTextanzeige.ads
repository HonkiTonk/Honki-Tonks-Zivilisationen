pragma SPARK_Mode (On);
pragma Warnings (Off, "*array aggregate*");

package InteraktionTextanzeige is

   ------------------- Später erweitern mit nur Schriftgröße setzen, nur Schriftfarbe setzen, usw..
   AccesseSetzen : Boolean := False;

end InteraktionTextanzeige;
